# @luciaocr/vue

Vue 3 friendly luciaocr package.

This package builds on top of `@luciaocr/web` and currently re-exports the browser SDK plus two Vue-oriented helpers:

- `createVueOCR(options?)`
- `useOCR(options?)`
