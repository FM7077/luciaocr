# @luciaocr/angular

Angular-friendly luciaocr package.

This package builds on top of `@luciaocr/web` and currently re-exports the browser SDK plus Angular-oriented helpers:

- `createAngularOCR(options?)`
- `provideLuciaocr(options?)`
- `injectLuciaocr()`
