# @luciaocr/react

React-friendly luciaocr package.

This package builds on top of `@luciaocr/web` and currently re-exports the browser SDK plus two React-oriented helpers:

- `createReactOCR(options?)`
- `useOCR(options?)`
